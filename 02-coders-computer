#The Coder's Computer

##Choosing a text editor

The main types of text editor are ones that are pre-installed on your computer and third-party options.

Usually, the pre-installed text editors do not have any features that make coding easier. These features include code completion, syntax highlighting, themes, and extensions.

In the end, you may need to try several text editors to find out which one is the best for you. Some software developers my find a feature essential that others can live without.


Command line is a text-based interface to the system. In MacOS, it’s an app called Terminal.

Basic Navigation
There are often multiple ways to do the same thing. Just use whatever you prefer.

File manipulation
Anything that you do via the GUI can be done in the command line. Creating, copying, moving, and deleting files can all be accomplished with a few commands. It’s important to remember that there his no undo on the command line, so be aware that changes are permanent.

Cheat Sheet
You can press the up arrow key to go back to commands you entered previously rather than typing them again.
Relative paths are based on where you currently are (PWD); Absolute paths are described starting from the root of the file system
Tab can auto-complete the path but also show a list of commands
touch - creates a blank file
To replace a file move (mv) one to an existing file name
rm = remove
rm -r = remove directory with files in it without individually deleting all of the files
man + command  will show you the instructions and options for that command
